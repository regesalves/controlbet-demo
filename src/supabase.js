import { createTestSupabaseClient } from "./test-support/mockSupabase";
export const supabaseConfigError = "";
export const isSupabaseConfigured = false;

export const supabase = createTestSupabaseClient();
